Temporaire readme
Interface GDB  écrit en Go en utilisant  GDB/MI à travers la bibliothéque github.com/cyrus-and/gdb

Pour l'instant:
-le programme débuggé doit être compilé en ligne de commandes au préalable et le nom de l'excécutable rentré dans le code 
du fichier Go_GDB.go
- les commandes disponibles :
  -break : breakpoints
  -break-list : listes des breakpoints
  -delete : supprime tous les breakpoints ou un sélectionné
  -run: fait un run du programme
  -step : avance d'un pas
  -step-reverse : recule d'un pas
  -continue: 
  -continue-reverse: 
  -print : imprime une valeur
  -list-variables: donnes la liste des variables
  -backtrace: la pile d'éxcution
  -watch : crée un watchpoint
  -where: indique où la ligne de code où se trouve 
  quit : arrete le débogage
